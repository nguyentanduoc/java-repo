info:
  name: create-order
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/order/createOrder
  body:
    type: json
    data: |-
      {
        "productId":1,
        "quantity":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
