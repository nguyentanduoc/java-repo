# Database Schema — spingddd

## 1. Schema là `src/main/resources/db/01-init-product.sql`
- Được dùng để khởi tạo dữ liệu trong **trang phát triển** (Profile `dev`).
- Khi chạy test (`application-test.yml`) có `ddl-auto: create-drop` làm mới schema.

## 2. Các bảng chính

| Tên bảng | Trường | Mô tả |
|---|---|---|
| `product` | `id BIGSERIAL`, `name VARCHAR(255)`, `price NUMERIC(12,2)` | Sản phẩm bán lẻ |
| `inventory` | `id BIGSERIAL`, `product_id BIGINT UNIQUE`, `available_quantity INT`, `version INT` | Thông tin tồn kho, `version` dùng để optimistic locking |
| `orders` | `id BIGSERIAL`, `product_id BIGINT`, `quantity INT`, `price DECIMAL(19,2)`, `amount DECIMAL(19,2)`, `user_id BIGINT`, `token VARCHAR(64)`, `created_at TIMESTAMP`, `updated_at TIMESTAMP`, `status SMALLINT` | Đơn hàng dành cho `Order` aggregate | `status` 0 = PENDING, 1 = COMPLETED |
| `outbox_event` | `id BIGSERIAL`, `aggregate_id VARCHAR(64)`, `event_type VARCHAR(64)`, `payload TEXT`, `status SMALLINT`, `created_at TIMESTAMP`, `published_at TIMESTAMP` | Tệp giao lưu bất đồng bộ (Event Store) |
| `idempotency_key` | `token VARCHAR(64)`, `created_at TIMESTAMP`, `expires_at TIMESTAMP` | Bảo vệ idempotency khi dùng Redis e.g. token + timeout |

## 3. Các Index & Constraints
- `orders` có `UNIQUE (token)`.
- `inventory` có `UNIQUE (product_id)`.
- `outbox_event` có index `idx_status_created` (status, created_at) để query efficient.
- `idempotency_key` có primary key (token).

## 4. Các Stored Procedures / Triggers (Nếu cần)
- Hiện tại không có stored procedure. Tất cả logic trừ, cập nhật tồn kho đều được xử lý trong Application Service, vì cần đồng bộ vừa nhanh (Redis) vừa chính xác (DB).

---
*Generated by Cline — 2026-09-13*
